package ssh_config

import "testing"

// CheckHostIP
//         If this flag is set to “yes”, ssh(1) will additionally check
//         the host IP address in the known_hosts file.  This allows ssh
//         to detect if a host key changed due to DNS spoofing and will
//         add addresses of destination hosts to ~/.ssh/known_hosts in the
//         process, regardless of the setting of StrictHostKeyChecking.
//         If the option is set to “no”, the check will not be executed.
//         The default is “yes”.
//

func TestCheckHostIPInitFunc(t *testing.T) {
	defer func() {
		if err := recover(); err != nil {
			t.Errorf("panic in test is not acceptable")
		}
	}()
	f, ok := mapInit[CheckHostIP]
	if !ok {
		t.Errorf("Cannot found init-function")
	}
	if f == nil {
		t.Errorf("Init-function is nil")
	}
	if f() == "" {
		// TODO
		t.Errorf("Not acceptable empty init value")
	}
}

func TestCheckHostIPValidFunc(t *testing.T) {
	defer func() {
		if err := recover(); err != nil {
			t.Errorf("panic in test is not acceptable")
		}
	}()
	f, ok := mapValid[CheckHostIP]
	if !ok {
		t.Errorf("Cannot found valid-function")
	}
	if f == nil {
		t.Errorf("Valid-function is nil")
	}
	if !f("") {
		// TODO
		t.Errorf("Not acceptable empty valid-value")
	}
}

func TestCheckHostIPParseFunc(t *testing.T) {
	defer func() {
		if err := recover(); err != nil {
			t.Errorf("panic in test is not acceptable")
		}
	}()
	f, ok := mapParse[CheckHostIP]
	if !ok {
		t.Errorf("Cannot found parse-function")
	}
	if f == nil {
		t.Errorf("Parse-function is nil")
	}
	// TODO
}
