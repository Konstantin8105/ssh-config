package ssh_config

import "testing"

// IgnoreUnknown
//         Specifies a pattern-list of unknown options to be ignored if
//         they are encountered in configuration parsing.  This may be
//         used to suppress errors if ssh_config contains options that are
//         unrecognised by ssh(1).  It is recommended that IgnoreUnknown
//         be listed early in the configuration file as it will not be
//         applied to unknown options that appear before it.
//

func TestIgnoreUnknownInitFunc(t *testing.T) {
	defer func() {
		if err := recover(); err != nil {
			t.Errorf("panic in test is not acceptable")
		}
	}()
	f, ok := mapInit[IgnoreUnknown]
	if !ok {
		t.Errorf("Cannot found init-function")
	}
	if f == nil {
		t.Errorf("Init-function is nil")
	}
	if f() == "" {
		// TODO
		t.Errorf("Not acceptable empty init value")
	}
}

func TestIgnoreUnknownValidFunc(t *testing.T) {
	defer func() {
		if err := recover(); err != nil {
			t.Errorf("panic in test is not acceptable")
		}
	}()
	f, ok := mapValid[IgnoreUnknown]
	if !ok {
		t.Errorf("Cannot found valid-function")
	}
	if f == nil {
		t.Errorf("Valid-function is nil")
	}
	if !f("") {
		// TODO
		t.Errorf("Not acceptable empty valid-value")
	}
}

func TestIgnoreUnknownParseFunc(t *testing.T) {
	defer func() {
		if err := recover(); err != nil {
			t.Errorf("panic in test is not acceptable")
		}
	}()
	f, ok := mapParse[IgnoreUnknown]
	if !ok {
		t.Errorf("Cannot found parse-function")
	}
	if f == nil {
		t.Errorf("Parse-function is nil")
	}
	// TODO
}
