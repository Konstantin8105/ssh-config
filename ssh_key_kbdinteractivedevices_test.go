package ssh_config

import "testing"

// KbdInteractiveDevices
//         Specifies the list of methods to use in keyboard-interactive
//         authentication.  Multiple method names must be comma-separated.
//         The default is to use the server specified list.  The methods
//         available vary depending on what the server supports.  For an
//         OpenSSH server, it may be zero or more of: “bsdauth”, “pam”,
//         and “skey”.
//

func TestKbdInteractiveDevicesInitFunc(t *testing.T) {
	defer func() {
		if err := recover(); err != nil {
			t.Errorf("panic in test is not acceptable")
		}
	}()
	f, ok := mapInit[KbdInteractiveDevices]
	if !ok {
		t.Errorf("Cannot found init-function")
	}
	if f == nil {
		t.Errorf("Init-function is nil")
	}
	if f() == "" {
		// TODO
		t.Errorf("Not acceptable empty init value")
	}
}

func TestKbdInteractiveDevicesValidFunc(t *testing.T) {
	defer func() {
		if err := recover(); err != nil {
			t.Errorf("panic in test is not acceptable")
		}
	}()
	f, ok := mapValid[KbdInteractiveDevices]
	if !ok {
		t.Errorf("Cannot found valid-function")
	}
	if f == nil {
		t.Errorf("Valid-function is nil")
	}
	if !f("") {
		// TODO
		t.Errorf("Not acceptable empty valid-value")
	}
}

func TestKbdInteractiveDevicesParseFunc(t *testing.T) {
	defer func() {
		if err := recover(); err != nil {
			t.Errorf("panic in test is not acceptable")
		}
	}()
	f, ok := mapParse[KbdInteractiveDevices]
	if !ok {
		t.Errorf("Cannot found parse-function")
	}
	if f == nil {
		t.Errorf("Parse-function is nil")
	}
	// TODO
}
