package ssh_config

import "testing"

// Protocol
//         Specifies the protocol versions ssh(1) should support in order
//         of preference.  The possible values are ‘1’ and ‘2’.  Multiple
//         versions must be comma-separated.  When this option is set to
//         “2,1” ssh will try version 2 and fall back to version 1 if ver‐
//         sion 2 is not available.  The default is ‘2’.  Protocol 1 suf‐
//         fers from a number of cryptographic weaknesses and should not
//         be used.  It is only offered to support legacy devices.
//

func TestProtocolInitFunc(t *testing.T) {
	defer func() {
		if err := recover(); err != nil {
			t.Errorf("panic in test is not acceptable")
		}
	}()
	f, ok := mapInit[Protocol]
	if !ok {
		t.Errorf("Cannot found init-function")
	}
	if f == nil {
		t.Errorf("Init-function is nil")
	}
	if f() == "" {
		// TODO
		t.Errorf("Not acceptable empty init value")
	}
}

func TestProtocolValidFunc(t *testing.T) {
	defer func() {
		if err := recover(); err != nil {
			t.Errorf("panic in test is not acceptable")
		}
	}()
	f, ok := mapValid[Protocol]
	if !ok {
		t.Errorf("Cannot found valid-function")
	}
	if f == nil {
		t.Errorf("Valid-function is nil")
	}
	if !f("") {
		// TODO
		t.Errorf("Not acceptable empty valid-value")
	}
}

func TestProtocolParseFunc(t *testing.T) {
	defer func() {
		if err := recover(); err != nil {
			t.Errorf("panic in test is not acceptable")
		}
	}()
	f, ok := mapParse[Protocol]
	if !ok {
		t.Errorf("Cannot found parse-function")
	}
	if f == nil {
		t.Errorf("Parse-function is nil")
	}
	// TODO
}
