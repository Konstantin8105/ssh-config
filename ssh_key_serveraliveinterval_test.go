package ssh_config

import "testing"

// ServerAliveInterval
//         Sets a timeout interval in seconds after which if no data has
//         been received from the server, ssh(1) will send a message
//         through the encrypted channel to request a response from the
//         server.  The default is 0, indicating that these messages will
//         not be sent to the server, or 300 if the BatchMode option is
//         set.  ProtocolKeepAlives and SetupTimeOut are Debian-specific
//         compatibility aliases for this option.
//

func TestServerAliveIntervalInitFunc(t *testing.T) {
	defer func() {
		if err := recover(); err != nil {
			t.Errorf("panic in test is not acceptable")
		}
	}()
	f, ok := mapInit[ServerAliveInterval]
	if !ok {
		t.Errorf("Cannot found init-function")
	}
	if f == nil {
		t.Errorf("Init-function is nil")
	}
	if f() == "" {
		// TODO
		t.Errorf("Not acceptable empty init value")
	}
}

func TestServerAliveIntervalValidFunc(t *testing.T) {
	defer func() {
		if err := recover(); err != nil {
			t.Errorf("panic in test is not acceptable")
		}
	}()
	f, ok := mapValid[ServerAliveInterval]
	if !ok {
		t.Errorf("Cannot found valid-function")
	}
	if f == nil {
		t.Errorf("Valid-function is nil")
	}
	if !f("") {
		// TODO
		t.Errorf("Not acceptable empty valid-value")
	}
}

func TestServerAliveIntervalParseFunc(t *testing.T) {
	defer func() {
		if err := recover(); err != nil {
			t.Errorf("panic in test is not acceptable")
		}
	}()
	f, ok := mapParse[ServerAliveInterval]
	if !ok {
		t.Errorf("Cannot found parse-function")
	}
	if f == nil {
		t.Errorf("Parse-function is nil")
	}
	// TODO
}
